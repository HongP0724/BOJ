#include <stdio.h>
#include <string.h>

int main(void)
{
    int t;
    while(t--)
    {
        char arr[51];
        scanf("%s",arr);
        int len = strlen(arr);
        int cnt = 0;
        for(int i = 0; i < len; i++)
        {
            
        }
    }
}